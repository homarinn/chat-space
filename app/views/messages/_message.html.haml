.message{data: {group: {id: message.group_id}, message: {id: message.id}}}
  .message__info
    %p.message__info__user-name= message.user.name
    %p.message__info__daytime= message.created_at.strftime("%Y/%m/%d(#{%w(日 月 火 水 木 金 土)[message.created_at.wday]}) %H:%M:%S")
  - if message.content.present?
    %p.message__text= message.content
  = image_tag message.image, height: '200', width: '200' if message.image.present?